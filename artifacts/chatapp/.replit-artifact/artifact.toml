kind = "web"
previewPath = "/"
title = "WhatsApp-Style Chat"
version = "1.0.0"
id = "artifacts/chatapp"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20227

[services.development]
run = "pnpm --filter @workspace/chatapp run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/chatapp", "run", "build" ]
publicDir = "artifacts/chatapp/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20227"
BASE_PATH = "/"
